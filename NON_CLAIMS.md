# Non-Claims

1. We do **not** claim a proof of the global Masur–Veech spectrum.
2. The values $\lambda_1^+=11/21$, $\lambda_2^+=1/3$ are a **conjecture** from numerical models of $X(2,0)$.
3. The sum $6/7$ is due to EKZ only.
4. Path-local exact $2:1$ orbits are not the measure average.
5. `promote_ready = false`.
